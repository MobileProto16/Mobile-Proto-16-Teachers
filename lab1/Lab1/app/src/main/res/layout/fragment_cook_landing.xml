<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.david.lab1.CookLandingFragment">

    <Button
        android:text="@string/view_edit_menu"
        android:layout_alignStart="@+id/orderButton"
        android:layout_alignEnd="@+id/orderButton"
        android:layout_alignLeft="@+id/orderButton"
        android:layout_alignRight="@+id/orderButton"
        android:layout_height="wrap_content"
        android:id="@+id/menuButton"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content" />

    <Button
        android:text="@string/see_orders"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:id="@+id/orderButton"
        android:layout_below="@+id/menuButton"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="@string/back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:id="@+id/backButton" />

</RelativeLayout>
